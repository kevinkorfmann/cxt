
Installation
============

Eventually, the package will be available on PyPI. Until then, you can install it.

.. code-block:: bash

    git clone git@github.com:kevinkorfmann/cxt.git
    pip install -r requirements.txt